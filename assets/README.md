# Assets

This folder stores packaging assets generated for the macOS bundle.

- `ClapTrigger.icns` is generated by `scripts/generate_icon.py`
- `ClapTrigger.iconset/` is a temporary build folder used before `iconutil`

The generated icon is not hand-drawn; it is created from a small scripted badge so CI and local builds stay reproducible.
